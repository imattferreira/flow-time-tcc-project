
import { Button, Container } from './styles';

function Pagination() {
  return (
    <Container>
      <Button>{ '<' }</Button>
      <Button> {'>'} </Button>
    </Container>
  );
};

export default Pagination;
